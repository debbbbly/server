port: 7880
log_level: info

rtc:
  tcp_port: 7881
  port_range_start: 6000
  port_range_end: 6100

redis:
  address: redis:6379

keys:
  api_key: ${LIVEKIT_API_KEY}
  api_secret: ${LIVEKIT_API_SECRET}

webhook:
  api_key: ${LIVEKIT_API_KEY}
  urls:
    - 'http://localhost:8080/livekit/webhook'