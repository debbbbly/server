spring:
  application:
    name: server
  datasource:
    url: jdbc:postgresql://debbly-db.cluster-cfwauugwq6ho.us-west-2.rds.amazonaws.com:5432/postgres
    username: dbuser
    password:
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml

server:
  port: 8080

livekit:
  url: wss://debbly.duckdns.org
  api-key:
  api-secret: