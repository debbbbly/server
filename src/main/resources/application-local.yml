# Local development profile
# Run with: ./mvnw spring-boot:run -Dspring-boot.run.profiles=local

livekit:
  egress:
    layouts:
      landscape: https://debbly.com/egress/16-9.html
      portrait: https://debbly.com/egress/9-16.html

cors:
  extra-allowed-origin-patterns:
    - "http://localhost:*"

logging:
  level:
    com.debbly.server: INFO
    # Keep framework noise at INFO/WARN
    org.springframework: INFO
    org.hibernate: WARN
    org.apache: WARN
    io.livekit: INFO